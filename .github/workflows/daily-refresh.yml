name: Daily LP Refresh

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Fetch and rebuild
        run: python3 build_lp.py
      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add lp_dashboard_standalone.html
          git diff --staged --quiet || (git commit -m "Daily refresh $(date +%Y-%m-%d)" && git push)
